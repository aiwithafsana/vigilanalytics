[pytest]
asyncio_mode = auto
# All async fixtures share the session event loop — prevents asyncpg
# "another operation is in progress" errors across fixture scopes.
asyncio_default_fixture_loop_scope = session
testpaths = tests
addopts = -v --tb=short
